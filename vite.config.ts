import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// https://vitejs.dev/config/
// No changes made to the existing configuration as per the instructions.
export default defineConfig({
  plugins: [react()]
});
